### Catalog table

Static or semi-static reference data. These are lookup tables that define the valid values or options used across the system. Rarely change after initial load.

`cat_`: cat_countries, cat_currencies
